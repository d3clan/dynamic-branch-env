# Virtual Environments Platform - System Architecture

High-level system architecture showing API Gateway, Lambda functions, DynamoDB, and AWS infrastructure provisioning flow

```mermaid
graph TB
    User[Developer/QA] --> API[API Gateway]
    API --> Auth[Lambda Authorizer]
    Auth --> Lambda[Environment Manager Lambda]
    Lambda --> DDB[(DynamoDB<br/>Environments Table)]
    Lambda --> CDK[CDK Deploy Lambda]
    CDK --> CFN[CloudFormation]
    CFN --> VPC[VPC Resources]
    CFN --> ALB[Application Load Balancer]
    CFN --> ECS[ECS Fargate Services]
    CFN --> RDS[(RDS Database)]
    Lambda --> R53[Route53]
    R53 --> DNS[env-name.domain.com]
    DNS --> ALB
    Lambda --> SNS[SNS Notifications]
    SNS --> Slack[Slack Integration]
```

---
*Generated by Technical Architect Agent*
